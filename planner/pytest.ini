[pytest]
# Планировщик запускается своим прогоном: пакеты engine и web у него
# собственные и столкнулись бы с одноимёнными пакетами Fiction Engine.
testpaths = tests
addopts = -q
filterwarnings = ignore::DeprecationWarning
