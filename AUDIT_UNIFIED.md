# Аудит fiction и unified: качество, проблемы, ключевые улучшения

2026-09-25 · ревизия `ece0528` · живая версия: https://claude.ai/code/artifact/4938b96a-3136-4c48-ad46-dddfb255be83

## Резюме

Код `fiction_engine` в хорошем состоянии. Слабое место проекта — `UNIFIED_ENGINE_MASTER` и его стык с кодом: почти половина базы знаний до модели не доходит, а часть доходящего неверна.

- **fiction_engine — 8/10.** 1254 теста зелёные, покрытие 76 %, mypy чистый. Дыры в вебе: DOCX-экспорт и импорт не работают из коробки, аварийная обрезка контекста отрезает State. Все эти пути без тестов.
- **UNIFIED — 4/10.** Как справочник по ремеслу текст сильный. Как рабочие данные для движка — слабый:
    - код читает 126 файлов из 233;
    - 43 % строк в модулях `03_ADVANCED_ENGINES` лежит в код-блоках, и экстрактор их выбрасывает;
    - 15 поджанров из 30 получают читательский контракт чужого поджанра.
- **Главный риск — стык, а не код.** Тесты проверяют загрузчики на синтетических файлах. Сверки «конфиг ↔ реальное содержимое UNIFIED» нет, поэтому ошибки сопоставления живут молча.
- **Определение жанра по подстроке ломается на обычных словах:** «конфликт интересов» → твёрдая НФ, «исторический детектив» → исторический любовный роман.

Ключевых улучшений шесть, они в разделе «Обязательные ключевые улучшения». Первые три закрывают большую часть риска и занимают 1–2 дня.

## Ход работ (на 26.09.2026)

Закрыты пункты 1 и 2 из обязательных улучшений, а также U2 и U5; U1 закрыт частично. Всё лежит в ветке `test/unified-contract` и ещё не влито в `master`. Тесты: 1528 проходят, 13 известных поломок помечены как ожидаемый провал.

| Пункт | Статус | Коммит | Что сделано |
| --- | --- | --- | --- |
| Улучшение 1: контрактный тест | сделано | `32d9795` | `tests/test_unified_contract.py`: 30 жанров против реальной базы. Известный долг помечен strict-xfail: после починки тест требует убрать запись. Без базы падает, а не пропускается |
| U1: чужие контракты | частично | `5666acc` | Исправлены 6 меток: три романтики, два «выживания», уютный детектив. Осталось 9 поджанров без своего раздела, это работа над текстом базы. Постапокалипсис намеренно не привязан к «ДИСТОПИИ»: тот раздел о системе, а не о выживании |
| U5: маркеры блоков | сделано | `d9251de` | Заголовки антиклише и проверки голоса вынесены в константы. Обрезка контекста и отладочный дамп берут их оттуда |
| U2: жанр по подстроке | сделано | `90d8acd` | Ключ ищется с начала слова, ключи до 5 символов — только целым словом. Убраны одиночные «городской» и «исторический». Инвариант в тестах: каждый ключ определяется как свой жанр |
| Улучшение 2: жанр в проекте | сделано | `95252d5` | Поле `projects.genre_key` и миграция для старых баз. Единая точка `project_genre_key()`. На главной — выбор жанра и подтверждение для старых проектов; без автора ничего не записывается |
| Новая находка: жанр текстом вместо ключа | сделано | `95252d5` | Судья получал «городское фэнтези» вместо ключа и не видел ни читательского контракта, ни жанрового чек-листа. Калибровка напряжения брала цели «default», жанровые усилители маршрутизатора не срабатывали. Затронуты все русскоязычные проекты |

Ещё не сделано:

- улучшение 3 — выгрузка, DOCX и обрезка контекста (F1–F3);
- улучшения 4–6 — машинно читаемый UNIFIED, снапшоты промпта, судьба нечитаемых файлов;
- долг в базе: 9 разделов контрактов и 4 раздела антагонистов (нуар и реализм).

Побочное действие границ слов: короткие ключи больше не ловят свои формы. «Шпионы» или «драмы» без другого ключа не определятся; это добирается списком форм.

## Что проверялось

Ревизия `ece0528`, ветка `master`. Все цифры получены запуском, а не осмотром.

| Проверка | Результат |
| --- | --- |
| `pytest --cov` (engine, web, cli) | 1254 passed, покрытие 76 % (6864 оператора, 1622 не выполнены) |
| `mypy engine web` | 0 ошибок, 53 файла |
| `pyflakes` | 342 предупреждения: 328 — реэкспорты фасадов, 3 мёртвые переменные |
| `check_architecture.py` | 1 предупреждение, допустимое |
| Трассировка чтения UNIFIED | 30 жанров × 3 режима × 8 типов задачи через `build_engine_context` + чек-листы + каталог + валидация путей |
| Сверка конфига с файлами | модули, профили, каталоги, метки контрактов, блоки промптов, арки, антагонисты — для каждого из 30 ключей |
| Собранный контекст | размеры блоков и шум для «детектив», «готический хоррор», «исторический роман» |
| `detect_genre` | 22 реалистичных формулировки жанра |
| Содержимое UNIFIED | валидность JSON/YAML, `INDEX.json` против диска, доля код-блоков, дубли между файлами, git-история |

Находки из `PROJECT_REVIEW.md` здесь не повторяются: отсутствие аутентификации, CSRF и ключи открытым текстом там уже зафиксированы.

## Оценка качества UNIFIED_ENGINE_MASTER

**Общая оценка — 4/10.** Содержание писалось как руководство для человека, который сам вставляет файлы в чат. README так и пишет: «загружай в контекст CORE_MINI + META_RULES…». Движок же вырезает куски регулярками по текстовым меткам, а база под это не перестроена. Отсюда большинство проблем.

| Критерий | Оценка | Основание |
| --- | --- | --- |
| Содержание (ремесло) | 7/10 | Конкретные правила и пары «слабо/сильно», жанровые варианты, хороший антиклише-словарь. Снижают маркетинговые вставки («Рейтинг 5++/5» — 19 раз) и описания несуществующей автоматики («автоматически» — 40 раз) |
| Машинная пригодность | 3/10 | 43 % строк модулей в код-блоках, экстрактор их выбрасывает. Секция MINI стоит в конце файла (на 71–97 % длины), а добор «тела» берёт начало файла — архитектурное предисловие |
| Согласованность с кодом | 3/10 | 15 из 30 меток контрактов не находятся; «критические» `CORE_FULL`, `CORE_MINI`, `META_RULES` код не читает |
| Покрытие кодом | 5/10 | Читается 126 файлов из 233 (54 %); из тех, что читаются, до модели доходят фрагменты |
| Структура и дубли | 5/10 | Дословных дублей мало (2,5 % длинных строк), но смысловых много: голос описан в 6 местах, напряжение и темп — в 5 |
| Сопровождение | 2/10 | Один коммит за всю историю; README — v1.0, `INDEX.json` — v1.8; `total_files` 226 против 233 на диске; README описывает папки 00–09 из 17 |
| Валидность данных | 9/10 | Все 39 JSON и YAML разбираются; все 225 записей INDEX есть на диске; каждый жанр из конфига имеет каталог |

### Что реально доходит до модели

Замер для «детектив», режим QUALITY, задача «диалог в кафе»: блок движка — 62 тыс. символов. В нём:

- 82 заголовка без содержимого: под «**Формула усталости:**» пусто, потому что сама формула была в код-блоке;
- 11 секций «🎯 НАЗНАЧЕНИЕ» в духе «динамическая система управления… автоматически рекомендует» — для генератора это шум;
- правила чужих жанров: «ХОРРОР:» и «РОМАНТИКА:» встречаются по 8 раз в промпте детектива;
- шкала интенсивности 1–10 дважды подряд в одном модуле (из MINI и из тела).

Из 12 статей антиклише в промпт попадают 5, все про эмоции. Каждая глава в каждом режиме получает одни и те же ~20 готовых фраз вроде «Дыхание сбилось. Руки стали холодными». Для длинной серии это прямой путь к новым клише, уже собственным. Файл `ai_cliches.md` (11 КБ, список ИИ-штампов) не читается вообще.

### Неиспользуемые файлы по папкам

Не читаются 107 файлов. Крупнейшие группы:

| Папка | Не читается | Что именно |
| --- | --- | --- |
| `05_CHARACTER_ENGINE` | 23 | все 12 стилевых профилей `STYLE/`, 7 жанровых (comedic, legal_thriller, medical_thriller, young_adult…), 3 голоса JSON |
| `11_PROMPTS` | 20 | все 14 шаблонов `templates/`, `*_generation.md`, `voice_lock.md`, `pace_fix.md` |
| `13_VOICE_LIBRARY` | 13 | все 11 жанровых профилей голоса |
| `08_STATE_ENGINE` | 7 | все файлы; State Engine живёт в SQLite |
| `00_CORE` | 6 | `CORE_FULL`, `CORE_MINI`, `META_RULES.yaml`, `ai_cliches`, `style_rules`, `cliches_list` |
| `07_SYSTEM_ENGINE` | 4 | все: описания «контроллера» и «обучения», которые в коде сделаны иначе или не сделаны |
| `06_PATTERN_LIBRARY` | 8 | все диалоговые паттерны, `universal_situations`, структуры глав |
| `02_STRUCTURE_ENGINE` | 5 | все |

Сильные стороны стоит сохранить: `anticliche_replacements.md` (замена вместо запрета), жанровые контракты с разделами «Обязательно / Нарушение», MINI-секции во всех 25 модулях, жанровые каталоги JSON.

## Проблемы UNIFIED и стыка с кодом

Все пункты воспроизведены запуском. Порядок — от самого серьёзного. Ни одна из этих проблем не ловится тестами.

### U1. Половина поджанров получает чужой читательский контракт — критично

`engine_loaders_genre.py:44` ищет секцию по метке из `SUBGENRE_CONTRACT_LABELS`. Если метка не найдена, фолбэк берёт первые 25 строк файла, то есть контракт **первого поджанра в файле**. Модель получает его как обязательный, в QUALITY и MASTER.

| Поджанр | Метка в коде | Секция в файле | Что получает |
| --- | --- | --- | --- |
| romance_historical | ИСТОРИЧЕСКИЙ | ИСТОРИЧЕСКАЯ РОМАНТИКА | современную романтику |
| romance_paranormal | ПАРАНОРМАЛЬНЫЙ | ПАРАНОРМАЛЬНАЯ РОМАНТИКА | современную романтику |
| thriller_survival | ВЫЖИВАНИЕ | ТРИЛЛЕР ВЫЖИВАНИЯ | психологический триллер |
| horror_survival | ВЫЖИВАНИЕ | ХОРРОР ВЫЖИВАНИЯ | психологический хоррор |
| detective_cozy | УЮТНЫЙ | КЛАССИЧЕСКИЙ / COZY | классический (случайно верно) |
| scifi_post_apocalyptic | ПОСТАПОКАЛИПСИС | ДИСТОПИЯ | твёрдую НФ |
| horror_cosmic, horror_gothic | КОСМИЧЕСКИЙ, ГОТИЧЕСКИЙ | нет (ближайшая — СВЕРХЪЕСТЕСТВЕННЫЙ) | психологический хоррор |
| fantasy_romantic, fantasy_sword_sorcery | — | нет | эпическое фэнтези |
| detective_psychological, detective_action | — | нет | классический детектив |
| scifi_cyberpunk, scifi_steampunk | — | нет | твёрдую НФ |

Семь случаев (с romance_contemporary: метка СОВРЕМЕННЫЙ, в файле СОВРЕМЕННАЯ РОМАНТИКА) исправляются правкой метки в `engine_config.py:301`, для восьми нужно написать разделы. Фолбэк «начало файла» нужно заменить на общие для семейства правила (в `fantasy_contracts.md` уже есть «УНИВЕРСАЛЬНЫЕ НАРУШЕНИЯ») или на пустую строку с записью в лог.

### U2. Жанр определяется поиском подстроки — критично

`unified_engine.py:42` проверяет `kw in genre_text` без границ слов. Короткие ключи срабатывают внутри обычных слов. Жанр переопределяется из свободного текста проекта при каждой генерации (`pipeline_context.py:165`), и ошибка меняет весь блок: каталог, правила, контракт, профиль, арку и набор модулей.

| Жанр проекта | Определено | Причина |
| --- | --- | --- |
| конфликт интересов / информационная война | scifi_hard | «нф» внутри «коНФликт», «иНФормация» |
| исторический детектив | romance_historical | «исторический» (12) длиннее «детектив» (8) |
| мистический детектив | horror_psychological | «мистический» длиннее |
| мыльная опера | detective_procedural | «опер» |
| ужасно смешная комедия | horror_psychological | «ужас» |
| городской роман | fantasy_urban | «городской» |
| научная фантастика с элементами детектива | scifi_hard | смешение жанров не поддержано, `GENRE_MERGER.md` не читается |
| комедия, литРПГ, боевик | None | нет ключей, хотя профиль `comedic.md` есть |

### U3. Экстрактор выбрасывает полезное и оставляет шум — высокая важность

`engine_extractors.py:62`, `_extract_body_lines`, в QUALITY/MASTER добирает к MINI первые строки тела модуля. В 25 из 25 модулей там стоит «🎯 НАЗНАЧЕНИЕ / КОМПОНЕНТЫ СИСТЕМЫ». Докстринг `_extract_module_essence` обещает обратное. Код-блоки вырезаются целиком, а в них лежат формулы и примеры: 43 % строк модулей, у `02`, `08`, `09` — более 65 %. Результат: сиротские заголовки и жанровые варианты чужих жанров. Фильтр «ценных» строк `_is_valuable` в продакшене не вызывается, его вызывают только тесты.

### U4. «Критические» файлы не читаются, читаемые не проверяются — высокая важность

`engine_loaders.py:52` объявляет `CORE_FULL.md`, `CORE_MINI.md`, `META_RULES.yaml` критическими, но ни один загрузчик их не открывает. При этом файлы, без которых блок движка реально теряет секции (`anticliche_replacements.md`, `16_GENRE_CONTRACT/*`, `11_PROMPTS/*_QUICK.md`), в списки не входят. Валидация при старте даёт ложную уверенность.

### U5. Маркеры блоков в коде разошлись с реальными заголовками — средняя важность

Антиклише пишется как «КЛИШЕ → ЗАМЕНЫ», проверка голоса — как «ПРОВЕРКА ГОЛОСА». А `pipeline_tasks.py:57–58` и отладочный дамп `pipeline_context.py:342` ищут «АНТИКЛИШЕ» и «ГОЛОС — ПРОВЕРКА». Итог: блоки не удаляются при обрезке, а дамп показывает «✗ engine_anticliche», хотя блок на месте. Маркера «ПОДЖАНР — обязательно» не создаёт никто.

### U6. Антиклише само порождает клише — средняя важность

`engine_loaders_core.py:33` берёт первые 50 непустых строк: 5 из 12 статей, все про эмоции. Блок заканчивается пустым заголовком «## ТИПОГРАФСКИЕ ПАТТЕРНЫ». Одни и те же готовые фразы идут в каждую главу. На дистанции серии модель начнёт повторять именно их.

### U7. Мелкие расхождения — низкая важность

- У нуара и реализма нет секции антагониста: ключи «НУАР» и «РЕАЛИЗМ» в `antagonist_by_genre.md` не найдены.
- У `detective_noir`, `fantasy_romantic`, `scifi_space_opera` и `scifi_steampunk` нет записи в `GENRE_MODULES`.
- Граф зависимостей продублирован в `INDEX.json` и `engine_config.py`, сейчас копии совпадают. При битом `INDEX.json` загрузчик тихо возвращается к хардкоду (`engine_loaders.py:149`).
- Бюджет блока (35 % от 180 тыс. токенов для Claude) в десятки раз больше реальных 13–62 тыс. символов. Обрезка по `MODULE_PRIORITY` на практике не срабатывает.

## Проблемы fiction_engine

Ядро движка (пайплайн, State, БД, API) покрыто на 85–100 %, и новых дефектов там не найдено. Проблемы сосредоточены в веб-слое и на границах контекста.

### F1. Выгрузка падает на кириллическом имени проекта — критично

`chapters.py:60` и `chapters.py:148` подставляют имя проекта в `Content-Disposition` как есть. Заголовки HTTP кодируются в latin-1. На реальном сервере Werkzeug это даёт `UnicodeEncodeError` и оборванный ответ — проверено запуском. Для русскоязычного инструмента это основной сценарий. TXT-экспорт «покрыт» тестами, но тестовый клиент Flask заголовки не кодирует, поэтому тест зелёный. Решение: `filename*=UTF-8''<percent-encoded>` по RFC 5987 или `send_file(..., download_name=...)`.

### F2. DOCX заявлен, но не работает из коробки — высокая важность

README: «загрузить уже написанные (`.txt`/`.docx`)». `python-docx` нет ни в `pyproject.toml`, ни в `requirements.txt`, ни в `.venv`. Экспорт (`chapters.py:79`) отвечает флэшем «Установи python-docx», импорт в State (`state_bp.py:63`) — ошибкой 500. Ни один путь не протестирован. После установки библиотеки экспорт упрётся в F1.

### F3. Обрезка контекста: пороги не согласованы, аварийный срез бьёт по State — высокая важность

- `pipeline_tasks.py:214` вызывает обрезку при оценке больше 90 тыс. токенов (270 тыс. символов). Но `_truncate_context_by_blocks` по умолчанию режет только свыше 360 тыс. символов. Контекст в 100 тыс. токенов уходит без изменений (проверено), при бюджете DeepSeek 100 тыс. и «default» 90 тыс.
- Последний резерв `result[:max_chars]` (`pipeline_tasks.py:100`) отрезает хвост. В хвосте `build_context` стоят символы и **СОСТОЯНИЕ ПЕРСОНАЖЕЙ**. Докстринг обещает «всегда сохраняет state».
- Из-за U5 антиклише и проверка голоса не удаляются, и порядок сброса приближает срез по State.

### F4. Блок движка может выпасть молча — средняя важность

`pipeline_context.py:184` ловит любое исключение сборки как RECOVERABLE и возвращает пустую строку. Именно так раньше жил «БАГ 1» из `PROJECT_REVIEW.md`. Ошибка пишется в лог, но пользователь и результат генерации этого не видят. Нужен флаг в `warning` генерации, как уже сделано для обрезки.

### F5. Тихие отказы после сохранения главы — средняя важность

`helpers.py:81` (генерация L3) и `helpers.py:139` (авто-оценка) гасят исключения без `log_web_error`. Соседние шаги той же функции логируют. При этом докстринг `log_web_error` прямо пишет, что именно так три бага прожили незамеченными. Модуль покрыт на 14 %.

### F6. Синхронные LLM-вызовы внутри HTTP-запроса — средняя важность

`pipeline_accept` (`generate_pipeline.py:131`) сначала помечает запуск принятым, потом в том же запросе сохраняет главу и вызывает `analyze_chapter`. `after_chapter_saved` делает до четырёх последовательных вызовов модели (L3, дрейф, заметка, оценка). Запрос висит десятки секунд. Если браузер или прокси оборвёт соединение, запуск останется принятым, а статус State автор не узнает.

### F7. Мелочи — низкая важность

- CLI: `int(prompt_input(...))` в `cli.py:158`, `218`, `278` падает с `ValueError` на нечисловом вводе. CLI покрыт на 12 %.
- Мёртвый код: `rhythm_hint` (`pipeline_tasks.py:266`), `knows_val` (`db_state.py:662`), `ok_medium` (`pipeline_steps.py:52`), двойной импорт `handle_error` (`pipeline_tasks.py:9–10`).
- 30+ `ResourceWarning: unclosed database` в тестах `test_unified_engine_helpers.py`: где-то соединение SQLite открывается без закрытия. Вероятный источник — `get_engine_path()`, который ходит в БД при каждом из ~15 вызовов загрузчиков на один промпт.

## Непокрытое тестами

Общие 76 % маскируют перекос: движок протестирован плотно, веб и стык с UNIFIED — слабо. Самая опасная дыра не видна в цифрах покрытия: загрузчики UNIFIED покрыты на 74–92 %, но на синтетических файлах. Проверки «для каждого из 30 жанров загрузчик находит свою секцию в реальной базе» нет ни одной.

| Модуль | Покрытие | Что не проверено | Чем грозит |
| --- | --- | --- | --- |
| Стык конфиг ↔ UNIFIED | — | метки контрактов, блоки промптов, антагонисты, маркеры секций | U1, U5, U7 — неверный или пустой контекст при зелёных тестах |
| `detect_genre` | 100 % строк | только позитивные примеры, ни одного «не должен совпасть» | U2 — чужой жанр для всей серии |
| Собранный промпт целиком | — | нет снапшота «жанр × режим → блоки и размеры» | U3, U6 — шум и дыры в промпте невидимы |
| `web/blueprints/helpers.py` | 14 % | `after_chapter_saved`, `_auto_score_chapter`, выбор дешёвой модели | F5 |
| `web/blueprints/chapters.py` | 37 % | DOCX-экспорт, поиск по главам, проверка непрерывности, L3-генерация | F1, F2 |
| `web/blueprints/generate_pipeline.py` | 49 % | accept / reject / continue с записью правок автора, автообновление State | F6; данные для DATA_DRIVEN_LEARNING могут не писаться |
| `web/blueprints/generate_prompt.py` | 49 % | сборка промпта для показа автору (строки 121–219) | автор видит не тот промпт, что уходит в модель |
| `web/app.py` | 61 % | настройки пути движка, стартовая валидация (347–414, 440–473) | неверный путь к UNIFIED — блок движка пуст |
| `engine/pipeline_context.py` | 65 % | KB-блок, эталоны голоса, отладочный дамп | U5 в дампе прожил бы незамеченным |
| `engine/pipeline_tasks.py` | 89 % | связка порога 90 тыс. токенов и дефолта 360 тыс. символов; сохранность State при hard_cut | F3 |
| `engine/continuity_checker.py` | 73 % | разбор ответа модели, ветки ошибок | ложные «нарушений нет» |
| `cli.py` | 12 % | почти всё | F7 |

Отдельно: есть тесты на мёртвый код (`_is_valuable`, 8 тестов). Они дают покрытие, но ничего не защищают. Тесты веба идут через тестовый клиент и не ловят ошибки уровня WSGI-сервера (F1).

## Обязательные ключевые улучшения

Шесть пунктов в порядке выполнения. Первые три занимают 1–2 дня и закрывают всё, что сейчас портит каждую генерацию. Мелкие правки (F7, U7) сюда не входят.

1. **Контрактный тест «конфиг ↔ реальный UNIFIED» и починка контрактов.** Закрывает U1, U5, U7.
    - Параметризованный тест по 30 жанровым ключам. Каждый загрузчик (контракт, блок промпта × 3 режима, арка, антагонист, профиль) должен вернуть секцию именно этого поджанра, а не фолбэк.
    - Маркеры из `DROP_ORDER` и отладочного дампа должны встречаться в реально собранном промпте. Лучше вынести заголовки блоков в одну константу и брать их и в сборке, и в обрезке.
    - Поправить 7 меток, дописать 8 разделов контрактов. Фолбэк «начало файла» заменить на общие правила семейства или пустоту с записью в лог.
    - Тест должен быть в CI и **падать**, а не пропускаться, если UNIFIED не найден.
2. **Жанр — явный ключ проекта, а не угадывание на каждой генерации.** Закрывает U2.
    - Хранить `genre_key` в проекте. Выбор — из списка `get_all_genre_options()`, функция уже есть. `detect_genre` оставить только для подсказки при создании проекта.
    - В `detect_genre` — совпадение по границам слов. Убрать или ограничить короткие и многозначные ключи («нф», «sf», «опер», «ужас», «городской», «исторический»). Добавить негативные тесты из таблицы U2.
    - Для существующих проектов — миграция: определить ключ один раз и показать автору для подтверждения.
3. **Исправить выгрузку и обрезку контекста.** Закрывает F1–F3.
    - `Content-Disposition` по RFC 5987 во всех трёх экспортах, плюс тест с кириллическим именем. Тест проверяет, что заголовок кодируется в latin-1.
    - `python-docx` — в зависимости (или extra `[docx]` с честной пометкой в README).
    - Один бюджет контекста от модели (`get_token_budget`) и для проверки, и для `max_chars`.
    - `hard_cut` должен резать середину, а State и задачу главы защищать.
4. **Сделать UNIFIED машинно читаемым, а не вырезать регулярками.** Закрывает U3 и U6. Это главное улучшение качества прозы и самое трудоёмкое.
    - В каждом модуле `03_ADVANCED_ENGINES` — явные секции `## PROMPT:QUICK` и `## PROMPT:FULL`. В них только инструкции генератору, формулы текстом, без «НАЗНАЧЕНИЯ» и код-блоков. Жанровые варианты — под маркерами семейства, чтобы в промпт шёл только свой жанр.
    - Экстрактор берёт только эти секции. Отсутствие секции — ошибка теста из п.1, а не тихий фолбэк.
    - Антиклише: все 12 категорий компактно («что передаёт → каким каналом заменить») плюс список из `ai_cliches.md`. Готовые фразы-примеры — ротация по номеру главы и прямой запрет копировать их дословно.
5. **Снапшот-тесты собранного промпта.** Закрепляет пп. 1–4.
    - Для 6–8 представительных пар «жанр × режим» сохранять в `tests/fixtures` список блоков, их размеры и число сиротских заголовков.
    - Любая правка UNIFIED или экстрактора показывает дифф промпта в ревью. Сейчас база и код меняются вслепую друг для друга.
6. **Решить судьбу 107 нечитаемых файлов и версионировать базу.** Закрывает U4.
    - Разделить на `runtime/` (то, что читает код) и `reference/` (руководства для человека), либо подключить ценное. В первую очередь: стилевые профили `STYLE/` и голоса `13_VOICE_LIBRARY` — как выбор в проекте, `ai_cliches.md`, `comedic.md`.
    - `_CRITICAL_PATHS` строить из того, что реально читают загрузчики.
    - Убрать маркетинг («5++/5», «автоматически» про несуществующее).
    - Одна версия в README и INDEX, `total_files` считается тестом. Дальше правки базы — отдельными коммитами с объяснением. Сейчас у UNIFIED всего один коммит.

Открытый вопрос: как часто авторы пишут смешанные жанры («НФ + детектив»)? Если часто, в п.2 нужен выбор двух ключей и подключение `GENRE_MERGER.md`.

## Решение: интегрировать и пересобирать ли UNIFIED

**Интегрировать — да, но только рабочую часть. Пересобирать — да, поэтапно, без переписывания с нуля.**

### Почему интегрировать

- Код и база связаны текстовыми метками (U1, U5). Правка одной стороны молча ломает другую, поэтому менять их нужно вместе: один коммит, один прогон тестов.
- Путь к базе считается от исходников: три уровня вверх от файла (`engine_loaders.py:17`). Обычная установка `pip install .` кладёт пакет в site-packages, и база там не находится. Работает только editable-режим, а `package-data` UNIFIED не включает.
- История разошлась: у кода 81 коммит, у базы — 1.

Как: рабочую часть перенести в `fiction_engine/engine/knowledge/` как package-data и читать через `importlib.resources`. Настройка `unified_engine_path` остаётся как переопределение. Справочную часть (руководства для человека, `07_SYSTEM_ENGINE`, `11_PROMPTS/templates`) держать отдельно как reference. Целиком не переносить: половину файлов код не читает, а внутри пакета они выглядели бы рабочими данными.

### Почему пересобирать и как

Содержание сильное (7/10), сломана форма. Целевой формат рабочей части:

- адресация по `genre_key` (`## horror_gothic` или файл на поджанр), а не по русским меткам. Это убирает весь класс ошибок U1 и таблицы `SUBGENRE_*_LABELS`;
- в модулях — секции `PROMPT:QUICK` / `PROMPT:FULL` и варианты по семействам жанров;
- один источник для зависимостей и версии.

Порядок:

1. Контрактный тест и снапшоты промпта (пп. 1 и 5 выше). Они фиксируют текущее поведение.
2. Переносить по одной группе: контракты → жанровые промпты → модули → антиклише. Каждый шаг — отдельный коммит с диффом собранного промпта.
3. После переноса удалить регулярки-экстракторы и таблицы меток.

Не делать: переписывать всю базу моделью за один проход. Потеряется точность авторских примеров, и нечем будет проверить, что стало лучше. Проверка результата — слепое сравнение глав до и после на 3–4 жанрах.
