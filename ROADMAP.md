# Путь к 10

Текущая оценка — **7,5 / 10** (см. [PROJECT_REVIEW.md](PROJECT_REVIEW.md)).
Здесь: что именно мешает десятке, в каком порядке это снимать и как проверить,
что сделано.

Правило для каждого пункта: **критерий проверяемый машиной**. «Стало лучше» —
не критерий. Если пункт нельзя проверить командой, он сформулирован плохо.

---

## Откуда взялись 7,5

| Направление | Сейчас | Что держит |
|---|---|---|
| Архитектура движка | 8 | `generate_bp.py` 905 строк, `pipeline.py` 858 |
| Качество кода | 8 | 40 блоков `except → pass`, mypy не строгий |
| Тесты | 7 | три параллельных набора, 14 450 строк дублирования |
| Гигиена / git | 7 | `planner.db` и 48 `.pyc` в первом коммите |
| Документация | 6 | README описывает 3 модуля, в `engine/` их 36 |
| Безопасность | 8 | нет аутентификации для сетевого режима |
| Эксплуатация | 7 | нет `pyproject.toml`, LICENSE, CI ни разу не отработал |
| `planner` | 5 | ноль тестов, чек-лист `PROJECT.md` не закрыт |

**Отдельно: три вещи не проверялись ни разу** — `pytest`, `mypy` и `flask`
в системе отсутствуют. Значит pytest-набор из 834 тестов не запускался,
типизация не измерялась, веб-приложение не стартовало. Это не «долг»,
это дыра в основании: часть оценки стоит на непроверенном.

---

## Этап 0. Убрать слепые зоны — ✅ ЗАКРЫТ (2026-09-11)

Пока это не сделано, остальные этапы двигают цифры вслепую.

| # | Задача | Критерий готовности |
|---|---|---|
| 0.1 | ✅ Окружение: `python3 -m venv .venv`, поставить `requirements-dev.txt` + mypy | `.venv/bin/pytest --version` отвечает |
| 0.2 | ✅ Прогнать pytest-набор целиком | **858/858, exit 0** (было 88 падений) |
| 0.3 | ✅ Прогнать mypy, зафиксировать базовую линию | **0 ошибок**, проверка в CI стала блокирующей |
| 0.4 | ✅ Поднять приложение, пройти по страницам | все страницы 200; обрыв проверен сквозь HTTP — API вернул `truncated: true` |
| 0.5 | ✅ Дождаться первого прогона CI | **зелёный**, обе задачи (checks, audit) |

**Почему первым.** 0.2 может обнаружить падения, которых я не видел: два вызова
`load_pattern_library` я правил статически. 0.4 — единственный способ убедиться,
что баннеры обрыва действительно рисуются, а не только приходят в JSON.

> **Результат 0.3 (2026-09-11).** mypy не запускался ни разу. Первый прогон —
> 22 ошибки, одна из них настоящий баг: `DBHandler.emit` звал
> `self.formatException`, которого у `logging.Handler` нет. AttributeError
> глотался, и записи НЕ доходили до `engine_error_log` именно тогда, когда
> логируется исключение. Это объясняет пустую таблицу при 65 ERROR в файле.
> Сейчас 0 ошибок, `continue-on-error` снят.
>
> **Результат 0.4 (2026-09-11).** Приложение поднято впервые. `/generate`,
> `/pipeline`, `/state` и ещё десяток страниц отдавали **500**: во всех
> стоял `url_for("main.index")`, а блупринта «main» в Fiction Engine нет —
> имя приехало из планировщика. 21 ссылка исправлена. Смоук-тест по всем
> GET-страницам (`tests/test_routes_smoke.py`) нашёл заодно сломанный
> шаблон `stats.html` — незакрытый `{% block content %}`. Обрыв главы
> проверен сквозь настоящий HTTP: `/generate/status` вернул
> `truncated: true, cut_reason: max_tokens, word_count: 2400`.
>
> **Результат 0.2 (2026-09-11).** Набор не был зелёным ни разу за историю проекта:
> на исходном коммите `cf1717f` — **88 падений из 855**. Сейчас **858/858**.
> Разбор причин и найденные попутно дефекты продукта — в коммитах `322c600`
> и `9fad114`. Ключевой урок: почти все падения — это тесты, отставшие от кода,
> и позиционные последовательности ответов LLM, которые съезжают при любом
> лишнем вызове. Заменены диспетчерами по роли и содержанию запроса
> (`tests/llm_stubs.py`).

---

## Этап 1. Документация → 10 — ✅ ЗАКРЫТ (2026-09-11)

| # | Задача | Критерий |
|---|---|---|
| 1.1 | ✅ Корневой `README.md` | три части, установка, схема потока, данные, переменные окружения |
| 1.2 | ✅ Переписан `fiction_engine/README.md` | все 35 модулей по слоям, 17 страниц, устройство |
| 1.3 | ✅ `LICENSE` | MIT |
| 1.4 | ✅ Описан CLI | все 9 команд из `COMMANDS` |
| 1.5 | ✅ `tests/test_docs_match_code.py` | 11 проверок; падение подтверждено на трёх сценариях расхождения |

1.5 — главное. Без него README снова отстанет на две итерации, как уже было.

> **Результат (2026-09-11).** Тест проверяет в обе стороны: каждый модуль,
> команда CLI и страница названы в README — и README не ссылается на то,
> чего нет. Плюс сверка счётчиков (модули, файлы базы знаний, блупринты) и
> проверка, что описанные переменные окружения действительно читаются кодом.
> Он сразу поймал мою собственную ошибку: «36 модулей» считало `__init__.py`.
> Работоспособность подтверждена тремя опытами — добавленный модуль без
> описания, ссылка на удалённый файл, недокументированная команда CLI:
> во всех трёх тест падает с внятным сообщением.
>
> Хрупкие числа (счётчики тестов) из текста убраны намеренно: они меняются
> на каждом коммите и превратили бы проверку в источник ложных падений.

---

## Этап 2. Тесты → 10 — ✅ ЗАКРЫТ (2026-09-11)

Сейчас три набора: `run_tests.py` (795 тестов, 9 118 строк), `tests/` (834 теста,
10 189 строк), четыре мёртвых `tests_*.py` в корне (5 332 строки). Одни и те же
проверки в двух-трёх реализациях, править надо синхронно.

| # | Задача | Критерий |
|---|---|---|
| 2.1 | ✅ Уникальные кейсы сведены в `tests/` | покрытие 71.4% → 77.8%, разрыв 470 → 134 строк |
| 2.2 | ✅ Удалены `run_tests.py` и корневые `tests_*.py` | −14 186 строк дублирования |
| 2.3 | ✅ `pytest-cov` в CI | покрытие печатается с term-missing |
| 2.4 | ✅ Порог покрытия | `--cov-fail-under=75` при текущих 77.8% |

**Осторожно с 2.2.** `run_tests.py` ценен тем, что работает без зависимостей —
моки `openai`/`anthropic` внутри. Именно поэтому он единственный, который я мог
запускать. Удалять только после того, как 0.2 подтвердит: pytest-набор реально
зелёный и покрывает то же.

> **Результат (2026-09-11).** Сравнение сделано по строкам кода, а не по
> названиям тестов: прогон каждого набора под coverage и разность множеств
> покрытых строк. Оказалось, что раннер уникально покрывал **470 строк** —
> удалить его вслепую значило бы потерять их.
>
> Перенесено четырьмя заходами (470 → 343 → 196 → 145 → 134) пятью новыми
> модулями в `tests/`: загрузчики против настоящей базы знаний, обрезка
> контекста по блокам, legacy-путь слияния State, логгер и клиенты
> провайдеров, динамический выбор модулей. Покрытие выросло с 71.4% до 77.8%.
>
> Остаток — **134 строки (2.2% кода)**: узкие ветки обработки ошибок и
> внутренности клиентов. Дальше отдача падала, а цена дубля оставалась
> прежней: за сессию одни и те же правки приходилось вносить дважды
> (тесты score_text, моки prefill, цель патча save_pipeline_iteration).
> Раннер удалён, покрытие защищено порогом в CI.
>
> Побочно: мёртвые `tests_*.py` в корне (5068 строк) содержали 244 теста,
> все до единого уже присутствовавшие в `run_tests.py`. Проверено пофамильно
> перед удалением.

---

## Этап 3. Качество кода → 10 — ✅ ЗАКРЫТ (2026-09-11)

| # | Задача | Критерий |
|---|---|---|
| 3.1 | ✅ Разобраны `except → pass` | **40 → 7**, все семь законные и задокументированные |
| 3.2 | ✅ `generate_bp.py` разнесён на 4 модуля | максимум в `web/blueprints/` — 304 строки |
| 3.3 | ✅ `pipeline.py` разделён | оркестратор **361 строка**, плюс `pipeline_llm` 163 и `pipeline_tasks` 378 |
| 3.4 | ✅ mypy строгий по модулям | `disallow_untyped_defs` на **22 модулях**, mypy exit 0 |
| 3.5 | ✅ Снят `continue-on-error` с mypy | сделано в этапе 0 вместе с 0.3 |

Порядок внутри этапа: 3.1 первым. Именно `except → pass` был общим знаменателем
всех трёх найденных багов — это 40 мест, где следующая поломка пройдёт молча.

> **Результат 3.1.** 21 место переведено на логирование, 6 сужено до конкретных
> типов исключений, 7 осталось — все внутри логгера и его аварийных путей:
> логирование не может пожаловаться на собственный отказ. `tools/pitfalls.py`
> научен отличать широкий перехват (`Exception`) от узкого
> (`json.JSONDecodeError` в цепочке разбора, `EOFError` на Ctrl+D) — второе
> нормальный приём, а не долг.
>
> **Результат 3.2 и 3.3.** Оба разделения сделаны без изменения публичных путей
> импорта. В web блупринт остался один — подмодули берут `bp` из `generate_bp`,
> поэтому `url_for("generate.pipeline_page")` в шаблонах продолжает работать.
> В движке `pipeline` реэкспортирует всё, что вынесено, а подмодули обращаются
> к `_call` и соседям через прокси, разрешающий имя в момент вызова: иначе
> сломались бы 85 подмен `engine.pipeline._call` в тестах.
>
> **Результат 3.4.** `disallow_untyped_defs` включён на 22 модулях — всех, где
> аннотированы все функции (было 3). Десять функций дотипизированы, чтобы
> закрыть восемь модулей, которым не хватало одной-двух аннотаций.
> Проверено, что режим действительно работает: добавленная нетипизированная
> функция роняет mypy, а не проходит молча.

---

## Этап 4. Эксплуатация → 10 — ✅ ЗАКРЫТ (2026-09-11)

| # | Задача | Критерий |
|---|---|---|
| 4.1 | ✅ `pyproject.toml` | `pip install -e .` и `pip install .` в чистом venv проверены |
| 4.2 | ✅ Убраны обходы `sys.path` | вне тестов не осталось; `web/` стал обычным пакетом |
| 4.3 | ✅ Версионирование и CHANGELOG | `0.4.0`, видна на `/settings` и в `fiction-engine version` |
| 4.4 | ✅ CI обязателен для master | защита ветки включена, `checks` и `audit` обязательны |

---

> **Результат этапа 4 (2026-09-11).** Версия не дублируется в коде: читается
> из метаданных пакета, при запуске из исходников отдаёт `0.0.0+src`. Две
> новые проверки роняют сборку, если версия в коде разойдётся с
> `pyproject.toml` или не будет описана в `CHANGELOG.md`.
>
> Защита ветки настроена с `enforce_admins: false` — владелец по-прежнему
> может пушить в master напрямую, обязательные проверки гейтят остальных
> и запрещают force-push. Снять: `gh api -X DELETE
> repos/qrewq5003-oss/fiction_engine/branches/master/protection`.

## Этап 5. Безопасность → 10

| # | Задача | Критерий |
|---|---|---|
| 5.1 | Аутентификация при `FE_HOST=0.0.0.0` | без пароля наружу не поднимается |
| 5.2 | Ключи API не открытым текстом в SQLite | в БД нет читаемого `sk-...` |
| 5.3 | CSRF-токены на POST-маршрутах | POST без токена отклоняется |
| 5.4 | Решить судьбу `planner.db` в истории `cf1717f` | либо репозиторий приватный, либо история переписана |

5.4 — решение, а не работа: если репозиторий приватный, достаточно записать это
решение. Если публичный, нужен `git filter-repo` и force-push.

---

## Этап 6. `planner` → 10 — ✅ ЗАКРЫТ (2026-09-11)

| # | Задача | Критерий |
|---|---|---|
| 6.1 | ✅ Тесты планировщика | **40 тестов**: данные, миграции, маршруты, статус |
| 6.2 | ✅ Чек-лист закрыт | 9 из 9 отмечены по факту, снят открытый вопрос об интеграции |
| 6.3 | ✅ Контракт с FE проверяется | 15 тестов на стороне FE, адреса вычитываются из кода планировщика |
| 6.4 | ✅ Чужая БД больше не читается | только HTTP; адрес настраивается через `FE_URL` |

6.3 не случайно: именно `/api/project/context` был сломан и молча возвращал
пустой список обещаний. Интеграция, на которую рассчитывает `planner`,
не проверена ничем до сих пор.

> **Результат (2026-09-11).** Стык оказался сломан в трёх местах сразу:
> `/api/inline_call` в Fiction Engine не существовало вовсе — ассистент
> планировщика всегда получал 503; `/api/director_note` звался без номера
> главы, и экспорт плана молча возвращал `sent: false`; `?pid` в
> `/api/project/context` игнорировался, то есть импортировался активный
> проект, а не запрошенный. Плюс `/fe/import` падал на импорте
> несуществующей функции `save_logline` — то есть не работал никогда.
>
> Отдельно: `apply_migrations()` не вызывался НИ ОТКУДА, хотя докстринг
> обещал запуск при старте. 136 строк мёртвого кода, а миграция 1 и не
> прошла бы: она строит индексы по таблицам `scene_tags` и `scene_beats`,
> которых нет ни в схеме, ни в других миграциях.
>
> Контрактный тест живёт на стороне FE и вычитывает адреса прямо из
> исходников планировщика: подмена адреса на несуществующий роняет его.

---

## Порядок

```
Этап 0 ──> Этап 1 ──> Этап 2 ──┐
   слепые    докум.    тесты   ├──> Этап 3 ──> Этап 4
   зоны                        │     код       упаковка
                               │
   Этап 5 (безопасность) и Этап 6 (planner) — независимы,
   можно брать в любой момент
```

Этап 0 блокирует всё: без запущенных pytest и mypy любая оценка после правок
снова будет «по внешним признакам» — ровно та ошибка, которая дала первые 6/10
при неработающем продукте. **Этап закрыт 2026-09-11.** Он окупился сразу:
pytest оказался красным с первого дня проекта (88 падений), mypy нашёл
неработающую запись исключений в БД, а первый запуск приложения показал 500
на большинстве страниц. Ни одно из трёх не было видно снаружи.

Этап 2 зависит от Этапа 0: удалять `run_tests.py` нельзя, пока не доказано,
что pytest-набор его заменяет.

Этап 3 идёт после Этапа 2: разбирать `except → pass` и резать крупные файлы
безопасно только под тестами, которым доверяешь.

---

## Как считается оценка

Десятка — не «всё сделано», а **каждое направление проверяется автоматически
и проверка зелёная**. Практически это значит: на любой коммит CI прогоняет
тесты, типы, границы слоёв, сигнатуры, ловушки и покрытие — и падает, если
что-то просело.

Сегодня из этого списка автоматически проверяются четыре пункта из шести
(тесты, границы, сигнатуры, ловушки), причём один из них — в режиме
«не блокировать».
