# История изменений

Формат опирается на [Keep a Changelog](https://keepachangelog.com/ru/1.1.0/),
версии — по [семантическому версионированию](https://semver.org/lang/ru/).

Проект дошёл до `0.3.0` без явного версионирования: первый коммит содержал
уже сложившуюся систему, а изменения фиксировались только в
`UNIFIED_ENGINE_MASTER/IMPROVEMENTS_CRITIQUE.md`. Отсчёт ведётся с `0.4.0` —
первой версии, у которой есть проверяемое основание: зелёные тесты, типы,
CI и документация, сверяемая с кодом автоматически.

---

## [0.4.1] — 2026-09-15

Версия про то, как движок себя проверяет. Внешних возможностей не
добавилось; найдено и починено девять мест, где проверка молча возвращала
правдоподобную ложь.

### Исправлено

- **Критику подсказывали ответ.** Замер ритма (`analyze_sentence_rhythm`)
  уходил в промпт критика под заголовком «ДАННЫЕ АНАЛИЗА» прямо перед
  текстом главы. Критик исправно называл ритм главной проблемой: с
  подсказкой — 5 случаев из 5, без неё — 0 из 5. Заслоняло это настоящие
  претензии: «глава — это не сцена, а экспозиция», «отсутствие
  сценического момента». Ритм с оценкой при этом не связан вовсе — по 17
  текстам `r = +0.42`, то есть чем БОЛЬШЕ коротких предложений, тем выше
  оценка. Замер остался в результате, из промпта убран.
- **Критик видел треть главы и судил о финале.** `score_text` слала
  `text[:4000]` — в среднем 36 % главы, местами 18 %, при задаче оценить
  «темп, крюк, ФИНАЛ, движение». Поймано на цитате: критик пожаловался на
  обрыв «Ты опозда», а в тексте «Ты опоздал на три минуты», и это символ
  3991 — граница обрезки. На оценку не влияло (структура −0.4 при
  разбросе 0.8), но исчезла ложная претензия, которую мы сами и создавали.
- **Проверка непрерывности видела четверть главы.** 3000 символов с
  комментарием «достаточно для проверки фактов» — утверждение, которого
  никто не проверял. Подсадка противоречия на 85 % главы: полный текст
  находит и называет метку дословно, выборка не находит. Факты рассыпаны
  по всей главе, выборка резала искомое.
- **Поиск символов видел четверть главы, и чинится парой чисел.** Обрезка
  3000 символов была подобрана под лимит ответа в 800 токенов: поднять
  одно без другого значило заменить тихую потерю (4 символа против 12) на
  исключение `call_json` и 500 пользователю. Оба числа теперь рядом, с
  объяснением связи.
- **Оценка стиля видела пятую часть главы.** 2000 символов — 18 %.
  Проверка подсадкой слома стиля, три повтора на клетку: слом, который
  оценка ВИДИТ, роняет её с 7.0 до 2.3. Сама проверка исправна — она
  просто смотрела не туда.
- **Пустой ответ модели принимался за текст.** `z-ai/glm-5.3` примерно в
  половине вызовов отдаёт пустое содержимое с `finish_reason='length'`
  (поведение провайдера — проверено и сырым HTTP, и через SDK). Шаг
  генерации сохранял пустую итерацию, `score_text` выставлял 0 из 50 —
  неотличимо от «модель написала плохо». Проверка поставлена в диспетчер
  `call_model`, а не в пять функций провайдеров.
- **Обрыв главы на полуслове не ловил никто.** Глава нужной длины,
  провайдер сообщил чистый `stop`, текст кончается на «и». Судья к такому
  слеп: срез в одной точке, разная только граница — обрыв посреди слова
  даёт ту же оценку структуры, что целый текст (+0.0 при разбросе 0.8),
  один текст даже вырос с 6 до 8. Завершённость проверяется арифметикой.
- **Главная претензия критика терялась в одной главе из шести.** Остаток
  дефекта, чиненного 13.09: критик иногда не использует свой заголовок, а
  перечисляет нарушения в разделе чеклиста. Разбор стал терпимее; отдельный
  тест стережёт, чтобы запасной путь не выдумывал претензию из любого
  текста.
- **Форма ответа поиска символов не гарантирована.** Модель возвращает то
  объект с `found`, то голый список; веб-слой делает `{"ok": True,
  **result}` — на списке это `TypeError` и 500.

### Изменено

- **Порог принятия главы: `≥ 30` и каждый критерий `≥ 4`** вместо `≥ 40` и
  `≥ 7`. Прежний не брала ни одна глава ни разу, и вердикт «НА ДОРАБОТКУ»
  выдавался всегда, то есть не значил ничего. Новые числа выбраны не по
  медиане (это была бы подгонка), а по разделяющей способности: порог
  должен принимать целую главу и отвергать испорченную. На нарочно
  испорченных текстах порог 25 пропускает 10 испорченных из 15, порог 32
  режет живое (2 настоящих из 15), порог 30 — 7 настоящих и 2 испорченных.
- **Числа, названные в промптах, сведены к константам:** норма ритма
  (30/50/20), требование к объёму (9 литералов), порог вердикта (стоял в
  двух местах разными значениями — 40 и 38). Копии в этом проекте уже
  расходились: «15-20 абзацев» против «25-35» на один и тот же объём.
- **Правило о деталях** вместо «Место: с одной атмосферной деталью» —
  промпт буквально заказывал украшение, за которое критик снижает оценку
  (15 однородных претензий из 15: «детали декоративны», «описан
  декоративно, но не ощутимо»). Сдвига оценки от этой правки не измерено.

### Добавлено

- **`tools/bench.py`** — повторяемый замер качества: фиксированный посев во
  временной БД (мерит движок, а не роман), хэши промпта генерации и
  промпта критика, предупреждение при сравнении замеров разными версиями
  измерителя. Режимы: обычный, отбор редакторов (`--editors`), парный
  замер выгоды от редактуры (`--edit-gain`).
- **Разброс замера измерен:** пять прогонов одной модели на одном промпте
  с одним судьёй дали 37 · 30 · 29 · 26. Стандартное отклонение 4.0,
  размах 11 из 50. По одному прогону различим только сдвиг от ~8 баллов —
  поэтому рейтинг моделей от 13.09 (33 против 32 против 29) был шумом, и
  оценки убраны из списка моделей.
- **Редактура на первой итерации** (`PIPELINE_GENERATE_AND_EDIT`): шаг
  `edit` видит текст и критику текущей итерации, а не только предыдущей.
  Набором по умолчанию НЕ становится: парный замер на 8 парах дал −0.1
  балла, то есть лишний вызов модели без отдачи.

---

## [0.4.0] — 2026-09-11

Версия ревизии. Внешних возможностей не добавилось; починено то, что
годами не работало, и выстроен контур, который не даст этому повториться.

### Исправлено

- **База знаний не попадала в промпты.** В `engine_loaders` у `load_module`
  единственного из всех загрузчиков не было обёртки, подставляющей путь
  движка: имя модуля уходило в параметр `engine_path`, дальше `str / str` →
  `TypeError` → перехват как `RECOVERABLE` → пустой блок. В логе
  пользователя 70 таких срабатываний. Контекст движка: `quick` 0 → 16 343,
  `quality` 0 → 37 057, `master` 0 → 48 633 символа.
- **Автообновление State Engine уничтожало анализ.** Читались колонки
  `new_global_state` и `new_plot_matrix`, которых в таблице нет: оба `.get()`
  давали `None`, состояние переписывалось само в себя, задача помечалась
  выполненной, а интерфейс сообщал об успехе. Применение переведено на
  `merge_analysis_into_state` — тот же путь, что у ручной кнопки.
- **Слияние анализа в State не работало ни на одном реальном формате.**
  Парсер искал `СОСТОЯНИЕ:`/`ЛОКАЦИЯ:` в верхнем регистре и
  регистрозависимо, тогда как форматов оказалось три (шаблон кода, шаблон
  базы знаний, авторский), а живые файлы хранятся в CRLF — из-за чего `$`
  в регулярных выражениях не совпадал вообще. Парсер стал терпимым:
  синонимы ключей, заголовки `### Имя` / `[ИМЯ — РОЛЬ]` / `**Имя**`,
  устойчивость к CRLF. Отчёт об изменениях выдаётся только при фактической
  правке текста — ложное «Применено N изменений» устранено.
- **Жанровая линза критика не применялась к русским жанрам:** таблица линз
  на английских ключах получала на вход «городское фэнтези» и молча
  откатывалась на нейтральную.
- **Исключения не доходили до `engine_error_log`:** `DBHandler.emit` звал
  `self.formatException`, которого у `logging.Handler` нет. Запись терялась
  ровно тогда, когда логируется исключение.
- **Страницы отдавали 500 при отсутствии проекта:** 21 ссылка на
  `url_for("main.index")`, тогда как блупринта `main` в проекте нет.
  Плюс незакрытый `{% block %}` ломал `/stats`.
- **Сортировка по `created_at` без вторичного ключа** в 8 местах: метка
  имеет секундную точность, поэтому «последнее обновление» и «свежие
  правки» врали при любой паре записей внутри одной секунды.
- `delete_project` чистил 6 таблиц из 17; `PRAGMA foreign_keys` не
  включался вовсе. Живая база пользователя содержала 11 осиротевших строк.
- `log_error` нарушал обещание «никогда не бросает»: аварийный путь ловил
  только `sqlite3.Error`.
- Потолок вывода стоял ниже требуемого объёма главы (`generate` 8000,
  `edit` 7000 при потребности 8 100–10 200 токенов на 3000 слов), причём
  редактура была теснее генерации. Бюджет выводится из целевого объёма.
- Обрыв ответа по лимиту токенов не распознавался: `stop_reason` и
  `finish_reason` отбрасывались всеми пятью провайдерами.

### Добавлено

- Распознавание обрыва главы и баннер «Продолжить главу» на страницах
  генерации и pipeline — подключён к уже существовавшему сохранению хвоста.
  Автозапуск сознательно не делается: повторный вызов модели стоит денег.
- `PRAGMA foreign_keys`, `journal_mode=WAL`, `busy_timeout` в соединении.
- Команда `fiction-engine cleanup` — удаление данных от удалённых проектов.
- Проект ставится пакетом: `pip install -e .`, команды `fiction-engine`
  и `fiction-engine-web`.
- CI на GitHub Actions: тесты с покрытием, mypy, границы слоёв,
  собственные AST-анализаторы в `tools/`.
- Документация: корневой `README.md`, переписанный `fiction_engine/README.md`,
  `LICENSE`, `PROJECT_REVIEW.md`, `ROADMAP.md` и тест, который роняет
  сборку при расхождении документации со структурой кода.

### Изменено

- Оба сервера по умолчанию слушают `127.0.0.1`, наружу — осознанно через
  `FE_HOST` / `PLANNER_HOST`.
- Ключ подписи сессий больше не берётся из плейсхолдера, лежащего в
  репозитории.
- `setup.sh` создаёт venv вместо установки в систему: на SteamOS и других
  системах с неизменяемым `/usr` прежний путь не работал в принципе.
- Тесты сведены в один набор: удалены `run_tests.py` и четыре
  неподключённых `tests_*.py` в корне, −14 186 строк дублирования.
  Уникальные случаи перенесены, покрытие выросло с 71.4% до 77.8%.
- Тихие перехваты исключений: 40 → 7, оставшиеся живут внутри логгера
  и задокументированы.
- `generate_bp.py` (905 строк) разнесён на четыре модуля, `pipeline.py`
  (858) разделён на оркестратор и два подмодуля — без изменения
  публичных путей импорта.
- Строгая типизация (`disallow_untyped_defs`) на 22 модулях вместо 3.

### Безопасность

- API-ключи по-прежнему хранятся в SQLite открытым текстом, а сетевой
  режим не требует аутентификации — см. этап 5 в [ROADMAP.md](ROADMAP.md).
- Рабочая БД планировщика и артефакты сборки убраны из индекса git.
  В истории первого коммита они остались.

---

## [0.3.0] и ранее — до 2026-07-10

Не версионировалось. Развитие описано в
`UNIFIED_ENGINE_MASTER/IMPROVEMENTS_CRITIQUE.md`: переход v3 → v4
с батчевой генерацией L3, очередью обновлений State Engine и
структурированными обещаниями.

[0.4.0]: https://github.com/qrewq5003-oss/fiction_engine/releases/tag/v0.4.0
